# vite-plugin-pwa has not yet declared vite 8 in its peer dep range, even though
# it is compatible. This flag lets npm resolve the tree without treating that
# undeclared-but-working combination as a hard error.
legacy-peer-deps=true
